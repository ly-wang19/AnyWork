interface:
  display_name: "Capture Decisions and Actions"
  short_description: "Extract decisions, owners, dates, and open questions"
  default_prompt: "Use $capture-decisions-actions to turn these meeting notes into an action record."
